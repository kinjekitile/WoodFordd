@model ReservationModel
@{
    ViewBag.Title = "Archive Reservation";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@section H1 {
    <h1>Archive Reservation</h1>
}

<div class="box box-primary">
    <div class="box-header with-border">
        <h3 class="box-title">Archive Reservation</h3>

    </div><!-- /.box-header -->



    @using (Html.BeginForm("Archive", "Reservations", FormMethod.Post, new { enctype = "multipart/form-data", @class = "form-horizontal", role = "form" })) {

        <div class="box-body">
            <p>Are you sure you wish to archive Reservation: @Model.Id ?</p>

            @Html.ValidationSummary()

            @Html.HiddenFor(x => x.User.Id)

            <div class="form-group">
                @Html.LabelFor(m => m.IsArchived, "Is Archive", new { @class = "col-sm-2 control-label" })
                <div class="col-sm-10">
                    @Html.CheckBoxFor(m => m.IsArchived)
                    @Html.ValidationMessageFor(m => m.IsArchived)
                </div>
            </div>

            <div class="form-group">
                <div class="col-sm-offset-2 col-sm-10">
                    <button type="submit" class="btn btn-primary">Archive Reservation</button>
                </div>
            </div>


        </div>
    }
</div>