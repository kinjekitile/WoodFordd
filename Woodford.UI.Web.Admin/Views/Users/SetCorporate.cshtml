@model UserModel
@{
    ViewBag.Title = "Set Loyalty Tier";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@section H1 {
    <h1>Woodford Users</h1>
}

<div class="box box-primary">
    <div class="box-header with-border">
        <h3 class="box-title">Set Corporate</h3>

    </div><!-- /.box-header -->



    @using (Html.BeginForm("SetCorporate", "Users", FormMethod.Post, new { enctype = "multipart/form-data", @class = "form-horizontal", role = "form" })) {

        <div class="box-body">
            <p>@Model.FirstName @Model.LastName</p>

            @Html.ValidationSummary()

            @Html.HiddenFor(x => x.Id)

            <div class="form-group">
                @Html.LabelFor(m => m.CorporateId, "Corporate", new { @class = "col-sm-2 control-label" })
                <div class="col-sm-10">
                    @Html.CorporateFor(m => m.CorporateId, new { placeholder = "Tier", @class = "form-control" })
                    @Html.ValidationMessageFor(m => m.CorporateId)
                </div>
            </div>

            <div class="form-group">
                <div class="col-sm-offset-2 col-sm-10">
                    <button type="submit" class="btn btn-primary">Update Corporate</button>
                </div>
            </div>


        </div>
    }
</div>