@model SearchResultsViewModel
@{
    ViewBag.Title = "Commercial Rates";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@section searchpanel {
    @{Html.RenderPartial("_SearchPanelPartial", Model.Criteria);
    }

}



<div class="row">
    <div class="twelve columns">
        <img src="@Url.Content("~/Content/images/commercialratesheader.jpg")" width="998" alt="Commercial Rates" />
    </div>
</div>


<div class="row">
    <div class="twelve columns">
        <h1>Commercial Rates</h1>
    </div>
</div>

<div class="row">
    <div class="twelve columns">
        <p>The key to the growth of the Woodford Group's growth from a two-car rental business in 1991 to the largest independant vehicle rental company in South Africa has been on offering a product based on flexibility and reliability.</p>

        <p>Woodford's commercial division is based on the same principles</p>

        <p>Our mission is to find the vehicle that suits your specifications and needs, and deliver your consignment or load to its destination with the least amount of fuss.</p>
    </div>
</div>


<div class="row">
    <div class="twelve columns">
        <h1>Book Below</h1>
    </div>
</div>

@{ Html.RenderPartial("_SearchResultsPartial", Model); }

    @section scripts {

        <script type="text/javascript" src="@Url.Content("~/Scripts/jquery.countdown.js")"></script>


        @{
            int expirySeconds = 0;
            if (Model.CountdownSpecial != null) {
                expirySeconds = Convert.ToInt32((Model.CountDownSpecialExpiry - DateTime.Now).TotalSeconds);
            }
        }
        @if (expirySeconds > 0) {
            <script type="text/javascript">
                $(document).ready(function () {
                    $(".count-down-special-container").show();

                    $('.count-down-special').countdown({
                        until: +@expirySeconds,
                        format: 'MS',
                        compact: true,
                        onExpiry: function () {
                            $('.count-down-special-container').hide();
                            $("label").removeClass("count-down-special-rate");
                        }
                    });
                });
            </script>
        }
        else {
            <script type="text/javascript">
                $(document).ready(function () {
                    $(".count-down-special-container").hide();
                    $("label").removeClass("count-down-special-rate");
                });
            </script>
        }

        <script type="text/javascript" src="@Url.Content("~/Scripts/jquery.format-1.3.min.js")"></script>

      
    }